import { ChangeHostElementDirective } from './change-host-element.directive';

describe('ChangeHostElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeHostElementDirective();
    expect(directive).toBeTruthy();
  });
});
