import { TextClickDirective } from './text-click.directive';

describe('TextClickDirective', () => {
  it('should create an instance', () => {
    const directive = new TextClickDirective();
    expect(directive).toBeTruthy();
  });
});
